# Copy this file to `.env.local` and fill the values with your real credentials.
# Do NOT commit `.env.local` — it should stay secret and is ignored by .gitignore.

# Cloudinary credentials (used for logo uploads)
# Obtain these from https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=dixlpimbc
CLOUDINARY_API_KEY=176892755844847
CLOUDINARY_API_SECRET=DogaG0e30ABaURU992nmtVgDcqg

# Optional: admin session secret (use a long, random string in production)
ADMIN_SESSION_SECRET=ThefirstBrianLogoAdminSessionSecretanditsVerySecure123!

# Optional alternative: single URL form supported by the Cloudinary SDK
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
